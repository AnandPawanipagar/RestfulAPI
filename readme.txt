first we have initiated node project using command -> npm init -y
With this command package.json is created.
Then we have created a app.js file.
Express is a node package which is used to create API
to install use command -> npm install express .
With this command node modules and package-lock.json will be created.